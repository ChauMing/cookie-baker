cookie transfer

解决开发时的登录权限问题（Manifest V3 + Vite + TypeScript）

##### usage:

1. clone this repo 并安装依赖: `pnpm install`
2. 开发: `pnpm dev`（启动 Vite dev server，支持 HMR）
3. 构建: `pnpm build`（产物输出至 `dist/`，并生成 `cookie-baker.zip`）
4. Chrome 打开开发者模式 --> 加载已解压的扩展程序 --> 选择 `dist/` 目录
